import StubPage from '../components/layout/StubPage';

export default function Reports() {
  return (
    <StubPage
      eyebrow="REPORTS"
      title="Cross-project reporting is coming soon"
      body="Roll-ups across all your scopes — readiness trend, coverage gaps, version history — will live here. For now, open a project to see its full report."
    />
  );
}
