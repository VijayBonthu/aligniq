import StubPage from '../components/layout/StubPage';

export default function Messages() {
  return (
    <StubPage
      eyebrow="MESSAGES"
      title="Direct messages are coming soon"
      body="We're building a unified inbox so collaborators can chat across projects without losing context. Until then, conversation history lives inside each project."
    />
  );
}
